<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-dark border-bottom shadow-sm">
  <h5 class="my-0 mr-md-auto font-weight-bold" style="    color: white;">###</h5>
  <nav class="my-2 my-md-0 mr-md-3">
    <span class="p-2 text-dark"><%= link_to 'Home', root_path %></span>
    </nav>
  <% if user_signed_in? %>
  <span class="btn btn-outline-primary"><%= link_to 'Logout', destroy_user_session_path, method: :delete %></span>
  <% else %>
  <span class="btn btn-outline-primary"><%= link_to 'Login', new_user_session_path %></span>
  <% end%>
</div>